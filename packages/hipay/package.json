{
  "name": "mn-hipay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "yarn g:lint",
    "test": "yarn g:test",
    "build": "yarn g:build",
    "release": "yarn g:publish"
  },
  "main": "./dist/index.mjs",
  "types": "./@types/index.d.ts",
  "dependencies": {
    "axios": "^1.6.7"
  }
}
